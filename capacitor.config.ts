import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-scale-garmin-connector',
  webDir: 'dist'
};

export default config;
